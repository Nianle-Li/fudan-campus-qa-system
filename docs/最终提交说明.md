# 最终提交说明

## 一、最终可运行版本

当前最终版本位于 `lll` 分支，包含数据库、后端、前端、测试脚本和展示材料。

正式运行流程：

```bash
createdb fcqa
psql -v ON_ERROR_STOP=1 -d fcqa -f db/init.sql
python -m pip install -r backend/requirements.txt
```

PowerShell 启动：

```powershell
$env:DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fcqa"
python backend/app.py
```

访问：

```text
http://127.0.0.1:8000
```

无 PostgreSQL 环境时可使用演示模式：

```powershell
$env:FCQA_DEMO_MODE="1"
python backend/app.py
```

## 二、最终功能清单

- 校区、建筑、设施、课程、活动查询。
- 建筑、设施、活动新增、修改、删除。
- 查询记录写入。
- 关键 SQL 查询文本与实际结果展示。
- 数据库初始化和初始测试数据导入。
- 自动冒烟测试和 SQL 验收查询。
- 最终课堂展示材料与自我测试说明。

## 三、最终检查结果

已完成检查：

- 后端语法检查：`python -m py_compile backend/app.py`
- 前端语法检查：`node --check frontend/app.js`
- Git 差异格式检查：`git diff --check`
- 演示模式冒烟测试：`python scripts/smoke_test.py --start-demo`

数据库模式检查方式：

- `psql -v ON_ERROR_STOP=1 -d fcqa -f db/init.sql`
- `psql -v ON_ERROR_STOP=1 -d fcqa -f db/verify_basic_queries.sql`

## 四、评审入口

- 项目 README：`README.md`
- 后端入口：`backend/app.py`
- 前端入口：`frontend/index.html`
- 数据库入口：`db/init.sql`
- 展示材料：`docs/最终展示材料.md`
- 自测说明：`docs/自我测试说明.md`
- 仓库整理说明：`docs/GitHub仓库整理说明.md`
